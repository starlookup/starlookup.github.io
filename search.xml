<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[逆向之路1——汇编基础]]></title>
    <url>%2F2017%2F08%2F23%2F2%2F</url>
    <content type="text"><![CDATA[本垃圾刚开始接触二进制，想做个逆向系列，记录我从0开始学习的过程，顺手做个摘录 寄存器32位寄存器|EAX|累加器|accumulator(累加)| |EBX|基址寄存器|base(基本)| |ECX|计数器|counter(计算)| |EDX|数据寄存器|data(数据)| |ESI|源变址寄存器|source index(源)| |EDI|目的变址寄存器|destination index(目的)| |EBP|扩展基址指针寄存器|base point(基址指针) |ESP|栈指针寄存器|stack point(栈指针)| |EIP|指令指针寄存器|instruction pointer(指令指针)| 标志寄存器好像这几个最重要 进位标志CF(Carry Flag):进位标志CF主要用来反映运算是否产生进位或借位。如果运算结果的最高位产生了一个进位或借位，那么，其值为1，否则其值为0。 零标志ZF(Zero Flag):零标志ZF用来反映运算结果是否为0。如果运算结果为0，则其值为1，否则其值为0。在判断运算结果是否为0时，可使用此标志位。 溢出标志OF(Overflow Flag):溢出标志OF用于反映有符号数加减运算所得结果是否溢出。如果运算结果超过当前运算位数所能表示的范围，则称为溢出，OF的值被置为1，否则，OF的值被清为0。 汇编命令我将资料上面的直接摘录写来了。。。 ADD (加)语法: ADD 被加数, 加数 加法指令将一个数值加在一个寄存器上或者一个内存地址上。 加法指令对ZF、OF、CF都会有影响。 AND (逻辑与)语法: AND 目标数, 原数 AND运算对两个数进行逻辑与运算。 AND指令会清空OF,CF标记，设置ZF标记。 CALL (调用)语法：CALL somethingCALL指令将当前的相对地址(IP)压入栈中，并且调用CALL 后的子程序 CMP (比较)语法: CMP 目标数, 原数 CMP指令比较两个值并且标记CF、OF、ZF DEC (自减)语法: DEC something dec用来自减1，相当于c中的– dec指令可以标记ZF、OF DIV (除)语法: DIV 除数 DIV指令用来将EAX除以除数(无符号除法)，被除数通常是EAX，结果也储存在EAX中，而被除数对除数取的模存在除数中。 div指令可以标记CF、OF、ZF INC (自加)语法: INC something INC同DEC相反，它是将值加1 INC指令可以标记ZF、OF JUMPSJA* - 如果大于就跳转(无符号) - CF=0 and ZF=0 JB* - 如果小于就跳转(无符号) - CF=1 JE** - 如果相等就跳转 - ZF=1 JG* - 如果大于就跳转(有符号) - ZF=0 and SF=OF (SF = Sign Flag) JGE* - 如果大于或等于就跳转(有符号) - SF=OF JL* - 如果小于就跳转(有符号) - SF != OF (!= is not) JLE* - 如果小于或等于就跳转(有符号 - ZF=1 and OF != OF JMP** - 跳转 - 强制跳转 JNE** - 如果不等于就跳转 - ZF=0 NOP (无操作)语法：NOP 这个指令说明不做任何事所以它在逆向中运用范围最广 POP语法：POP 目的地址 POP指令将栈顶第一个字传送到目的地址。 每次POP后，ESP(栈指针寄存器)都会增加以指向新栈顶 PUSH语法：PUSH 值 PUSH是POP的相反操作，它将一个值压入栈并且减小栈顶指针值以指向新栈顶。 RET (返回)语法：RET RET指令的功能是从一个代码区域中退出到调用CALL的指令处。 TEST语法：TEST 操作符、操作符这个指令99%都是用于”TEST EAX, EAX”，它执行与AND相同的功能，但是并不储存数据。如果EAX=0就会标记ZF，如果EAX不是0，就会清空ZF XOR语法：XOR 目的数,源数 XOR指令对两个数进行异或操作 这个指令清空OF、CF，但会标记ZF]]></content>
      <categories>
        <category>-study</category>
      </categories>
      <tags>
        <tag>-study</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[2017网络安全实验班选拔writeup]]></title>
    <url>%2F2017%2F06%2F29%2F1%2F</url>
    <content type="text"><![CDATA[先膜一遍前面的师傅，自己还是太垃圾，当时选拔没办法记录，所以都只是大概过程 挑战题Misc编程小练习我直接用记事本打开，通过记事本的功能找出4个字母为Ayke,唯一不爽的事，我以为是直接提交，结果发现一直不对，耽误很长时间后，用flag{Ayke}提交发现对了，一口老血 真有这么难用binwalk分析发现 其中有个flag.txt于是解压找出flag 网络007根据题目提示应该是个数据包，加个.pcap后缀，用wireshark打开，我突发奇想搜索关键字password运气比较好，发现传输了.png这个图片，于是我在搜索关键字找到这个图片 Web抽奖这题是陕西省网络空间安全技术大赛的原题，里面有个函数(function() { window.getFlag=function(text){ if(text==&#39;1&#39;){ alert(&quot;你最厉害啦!可惜没flag&quot;) } if(text==&#39;2&#39;){ alert(&quot;你太厉害了,竟然是二等奖&quot;) } if(text==&#39;3&#39;){ alert(&quot;你好厉害,三等奖啊&quot;) } if(text==&#39;flag&#39;){ alert(&quot;flag{951c712ac2c3e57053c43d80c0a9e543}&quot;) } if(text==&#39;0&#39;){ alert(&quot;再来一次吧&quot;) } } }) Web5刚开始真的没有头绪，于是无意搜索了一下robots.txt,结果有惊喜 于是复制路径得到看似很奇怪的东西，其实放在浏览器控制台跑一下就出来了 Crypt在各位师傅写脚本的时候解仿射密码题，由于本人没想到怎么写脚本，于是用手算的。。。可谓是非常搞笑了。。。]]></content>
      <categories>
        <category>-writeup</category>
      </categories>
      <tags>
        <tag>-writeup</tag>
      </tags>
  </entry>
</search>